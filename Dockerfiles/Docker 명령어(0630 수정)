1. Dockerfile 빌드 명령어
docker build --tag 이미지명:태그 .  << 마지막 점(.) 매우 중요! 경로 지정하는 부분.

2. 빌드한 이미지 컨테이너로 띄우는 명령어
docker run --name 컨테이너명 -d   << -d 옵션은 백드라운드 실행 옵션

3. 생성된 도커 이미지 보는 명령어
docker images

4. 컨테이너 확인명령어
docker ps / docker ps -a    << -a 옵션주면 종료된 컨테이너까지 보여줌

5. 도커 이미지 한번에 지우는 명령어
docker rmi $(docker images -q)

6. 도커 컨테이너 삭제 명령어
docker rm 컨테이너id  << 컨테이너id는 4번 명령어를 통해 확인 가능.

7. 도커 컴포즈 컨테이너들 백그라운드로 띄우는 명령어
docker-compose up -d

8. 도커 컴포즈 컨테이너들 내리는 명령어
docker-compose down

9. 도커 컴포즈 컨테이너들 로그 확인 명령어
docker-compose logs -f

10. bash를 통해 컨테이너 접근하는 명령어
docker exec -it 컨테이너명 /bin/bash
